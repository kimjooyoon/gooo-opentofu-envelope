# Gooo OpenTofu Envelope

This repository is bootstrapped for a small, read-only envelope between a
Gooo intent and pinned OpenTofu machine-readable observations.

The default branch intentionally contains only this README, the MIT license,
and repository hygiene rules. The implementation will be introduced by a
reviewed pull request and exercised only by GitHub Actions.
